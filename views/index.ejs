<!DOCTYPE html>
<html lang="en">
    <%- include("../partials/head.ejs") %>
<body>
   
  
    <%- include("../partials/nav.ejs") %>

    <div class="blogs content">
        <h2>all blogs</h2>
        
            <% if (reslt.length >0) { %> 
           <% reslt.forEach(blog => { %>
            <h3 class="="title> <%= blog.title %> </h3>
           <p class="snippet"> <%= blog.snippet %> </p>


            <% })   %>  

            <% } else { %>
                <h3>there are no blogd to display </h3>
                <% } %>

    </div>
    <%- include("../partials/footer.ejs") %>
</body>
</html>